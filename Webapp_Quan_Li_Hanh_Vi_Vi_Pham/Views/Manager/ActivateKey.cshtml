@{
    ViewData["Title"] = "Kích hoạt tài khoản Quản lý";
    Layout = "_Layout";
}

<div class="min-h-screen flex items-center justify-center bg-gradient-to-tr from-slate-900 via-red-950 to-slate-900 px-4 sm:px-6 lg:px-8 relative overflow-hidden py-12">
    <!-- Glowing circles -->
    <div class="absolute w-[500px] h-[500px] bg-red-500/20 rounded-full blur-[120px] -top-40 -left-40 animate-pulse"></div>
    <div class="absolute w-[400px] h-[400px] bg-rose-500/20 rounded-full blur-[100px] -bottom-40 -right-40 animate-pulse duration-5000"></div>

    <div class="max-w-md w-full space-y-6 glass-panel p-8 rounded-3xl shadow-2xl relative z-10 border border-white/10">
        <div class="text-center">
            <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-red-600/10 text-red-400 mb-3 border border-red-500/20 shadow-inner">
                <i class="fa-solid fa-key text-2xl"></i>
            </div>
            <h2 class="font-outfit text-2xl font-extrabold tracking-tight text-white">Yêu cầu kích hoạt</h2>
            <p class="mt-1 text-sm text-slate-400">Tài khoản quản lý của bạn cần được kích hoạt bằng khóa doanh nghiệp.</p>
        </div>

        @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.Any(v => v.Errors.Any()))
        {
            <div class="bg-red-500/10 border border-red-500/20 text-red-300 text-sm rounded-xl p-4 flex items-start space-x-2">
                <i class="fa-solid fa-triangle-exclamation mt-0.5 text-red-400"></i>
                <div>
                    @foreach (var modelError in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p>@modelError.ErrorMessage</p>
                    }
                </div>
            </div>
        }

        <form asp-action="ActivateKey" method="post" class="space-y-5">
            <input type="hidden" name="username" value="@ViewBag.Username" />

            <div class="bg-slate-950/20 border border-white/5 rounded-xl p-4 text-center">
                <span class="text-xs text-slate-400">Tài khoản kích hoạt:</span>
                <p class="text-sm font-bold text-red-400">@ViewBag.Username</p>
            </div>

            <div>
                <label for="key" class="block text-xs font-semibold text-slate-300 mb-2">Khóa kích hoạt (Corporate Key)</label>
                <div class="relative">
                    <span class="absolute inset-y-0 left-0 flex items-center pl-3.5 text-slate-500">
                        <i class="fa-solid fa-unlock-keyhole"></i>
                    </span>
                    <input type="password" id="key" name="key" required 
                           class="block w-full pl-10 pr-4 py-3 bg-slate-950/35 border border-white/10 rounded-xl text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent text-sm transition" 
                           placeholder="Nhập mã khóa doanh nghiệp..." />
                </div>
                <p class="text-[10px] text-slate-500 mt-1.5">Mã mặc định khởi tạo là `hieudeptraivcl` nếu chưa được Admin thay đổi.</p>
            </div>

            <button type="submit" class="w-full py-3 bg-red-600 hover:bg-red-700 text-white rounded-xl text-sm font-semibold shadow-lg shadow-red-600/30 hover:shadow-red-600/40 hover:-translate-y-0.5 transition duration-200">
                Xác thực & Kích hoạt
            </button>
        </form>

        <div class="border-t border-white/5 pt-4 text-center">
            <a asp-controller="Account" asp-action="Logout" class="text-xs font-semibold text-slate-400 hover:text-slate-300 transition">Quay lại Đăng nhập</a>
        </div>
    </div>
</div>
